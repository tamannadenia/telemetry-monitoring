{
  "name": "telemetry-monitoring",
  "version": "1.0.0",
  "private": true,
  "description": "Industrial IoT telemetry monitoring platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:setup": "npm run db:generate --prefix backend && npm run db:push --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:simulator": "npm run simulator --prefix backend"
  }
}
